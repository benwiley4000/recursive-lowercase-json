{
  "name": "recursive-lowercase-json",
  "version": "0.2.0",
  "description": "Small function recursively converts object keys to lowercase within JSON-compatible object.",
  "main": "dist/index.js",
  "scripts": {
    "build": "mkdir -p dist && babel src/index.js --out-file dist/index.js",
    "dev": "mkdir -p dist && babel src/index.js --watch --out-file dist/index.js"
  },
  "keywords": [
    "recursive",
    "json",
    "objects",
    "arrays",
    "keys",
    "lowercase",
    "transform"
  ],
  "author": "Ben Wiley",
  "license": "MIT",
  "dependencies": {},
  "repository": {
    "type": "git",
    "url": "git+https://github.com/benwiley4000/recursive-lowercase-json.git"
  },
  "bugs": {
    "url": "https://github.com/benwiley4000/recursive-lowercase-json/issues"
  },
  "homepage": "https://github.com/benwiley4000/recursive-lowercase-json#readme",
  "devDependencies": {
    "babel-cli": "^6.8.0",
    "babel-preset-es2015": "^6.6.0"
  }
}
